//write a program to calculate sum of digit

#include<iostream>
using namespace std;
int main(){
	int num,sum=0, rev;
	cout<<"enter a number"<<endl;
	cin>>num;
	while(num!=0){
		rev=num%10;//639
		
		num=num/10;
		sum=sum+rev;
	}
	cout<<sum;
	return 0;
}
