// Insert an element at the last of an array
#include<iostream>
using namespace std;
int main(){
	int temp, i;
	int arr[]={2,63,9,1,3,5};
	int n=sizeof(arr)/sizeof(arr[0]);
	cout<<"enter an element you want to insert "<<endl;
	cin>>temp;
	arr[n]=temp;
	n++;
	for(i=0;i<n;i++){
		cout<<arr[i]<<ends;
	}
	return 0;
}
