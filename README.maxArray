
// find maximum element of an array

#include<iostream>
using namespace std;
int main(){
    int i;
	int arr[]={2,7,5,9,8,4};
	int n=sizeof(arr)/sizeof(arr[0]);
	int max=arr[0];
    for(i=1;i<n;i++)
	if(max<arr[i])
	{
		max=arr[i];
	}
	cout<<max;
	return 0;
}
